# Einzelne Bausteine

Dieser Ordner enthält neutrale Vorlagen für einzelne Workflow-Dateien. Er ist **nicht** der eigentliche Workflow.

Wer das vollständige System verwenden möchte, arbeitet mit den Dateien direkt unter `Template/.opencode/` und `Template/.project/`.
